using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DefenseTypeThreeController : MonoBehaviour
{
    public GameObject bullet;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // Find the closest enemy

        // Target the enemy: shootType3 should be heading towards the enemy

        // Shoot the enemy: must calculate the expected position of the enemy

        // Destroy the bullet if it goes out of bounds

        // Destroy the bullet if it hits the enemy + audio effect
    }
}
